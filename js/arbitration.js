import { Fighter } from "./fighter.js";

/**
 * Arbitrate between two fighters.
 *
 * @param {Fighter} fighter1 The first fighter
 * @param {Fighter} fighter2 The second fighter
 * @returns {boolean} The result of the arbitration
 */
export default function arbitrate(fighter1, fighter2) {
  // Should return false if the difference between the scores of the two fighters too high
  return true;
}
